apiVersion: apps/v1beta2
kind: ReplicaSet
metadata:
  name: rickandmorty-develop
  namespace: rickandmorty
  labels:
    source: demo
    stack: develop
    app: rickandmorty-develop
spec:
  replicas: 2
  selector:
    matchLabels:
      source: demo
      app: rickandmorty
  template:
    metadata:
      labels:
        source: demo
        app: rickandmorty
        version: develop
    spec:
      containers:
      - name: rickandmorty
        image: gcr.io/cf-spinnaker/spinnaker-demo
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /
            port: 8080
        env:
          - name: DEPLOYMENT_TYPE
            value: "DEVELOP"
        volumeMounts:
        - name: datadog
          mountPath: "/etc/datadog-secret"
          readOnly: true
      volumes:
      - name: datadog
        secret:
          secretName: datadog-secret

---

kind: Service
apiVersion: v1
metadata:
  name: rickandmorty
  namespace: rickandmorty
spec:
  selector:
    source: demo
    app: rickandmorty
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer